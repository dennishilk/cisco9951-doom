# Release payload

The physically verified distribution is named:

```text
Cisco9951-doom.doompkg
```

The intended public release tag is **v.666**.

Verified package metadata for the final physically tested file:

```text
Size:    11,881,944 bytes
SHA-256: 2f0c29fa6213bf0cdb0a5cb9124d083c1ccff54ce4009730917c371d784d4dbd
```

The matching checksum is tracked in `Cisco9951-doom.doompkg.sha256`.

The package should be attached to the GitHub release rather than duplicated as a normal repository blob. The final proof media can be added alongside the release later.

The package uses Freedoom Phase 2 as its redistributable IWAD and does not include Cisco firmware, proprietary Cisco libraries or a commercial Doom IWAD.
